{
  "name": "dashboard-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Metrics Dashboard - frontend (Vite build). Backend lives in backend/.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node frontend/test/run.mjs",
    "typecheck": "tsc --noEmit",
    "typecheck:backend": "tsc -p backend/tsconfig.json",
    "verify": "npm run typecheck && npm run typecheck:backend && npm test && npm run build"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  }
}

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the backend (Admin SDK bypasses rules).
    // Clients must NOT read or write Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
